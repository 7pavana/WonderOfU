# WonderOfU persistent rules

Work only under `C:\Projects\WonderOfU`; keep raw data under configurable external `C:\WonderOfU_Data`. The project studies fraud/authenticity detection, not legal identity ownership. Preserve the unified visual+rPPG+document/shared-representation architecture and independent baselines. Never fabricate labels, availability, metrics, results, or predictions; use explicit manifests and source-disjoint splits. Do not bypass access controls or commit datasets, identity documents, uploads, checkpoints, or secrets. Test changed code, then update `PROJECT_STATUS.md` with only verified facts.

Python code belongs in `ml/`, HTTP in `backend/`, UI in `frontend/`, executable helpers in `scripts/`, and durable design notes in `docs/`. Keep configuration-driven paths and optional dependencies clearly reported.
